# files

This directory contains static support files used by repo scripts and host bootstrap flows.

---

## In this directory

### [com.startup.sysctl.plist](./com.startup.sysctl.plist)

Launchd plist used by the macOS host bootstrap steps to raise file-descriptor limits at startup.
